{
  "name": "switchr-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=3000 --ignore=New,old,README.md,package.json,package-lock.json"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
